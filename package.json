{
  "name": "sportzfight.com",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "emu": "firebase emulators:start --only firestore,auth --project demo-sportzfight",
    "test": "vitest run --exclude \"tests/{rules,matchmaking,e2e-flow}.test.ts\"",
    "test:rules": "firebase emulators:exec --only firestore --project demo-sportzfight \"vitest run tests/rules.test.ts\"",
    "test:e2e": "vitest run tests/e2e-flow.test.ts",
    "test:all": "firebase emulators:exec --only firestore,auth --project demo-sportzfight \"vitest run\"",
    "deploy:rules": "firebase deploy --only firestore:rules,firestore:indexes",
    "setup:mediapipe": "node scripts/setup-mediapipe.mjs",
    "postinstall": "node scripts/setup-mediapipe.mjs",
    "test:mm": "firebase emulators:exec --only firestore,auth --project demo-sportzfight \"vitest run tests/matchmaking.test.ts\"",
    "check:launch": "vitest run tests/legal.test.ts --config vitest.launch.mts"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35",
    "@sentry/nextjs": "^10.71.0",
    "firebase": "^12.18.0",
    "firebase-admin": "^14.3.0",
    "next": "16.3.2",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "server-only": "^0.0.1",
    "stripe": "^22.5.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.2",
    "firebase-tools": "^15.28.1",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
